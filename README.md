# Asynchronous-FIFO-
An Asynchronous FIFO (First-In First-Out) is a memory buffer used to safely transfer data between two systems that operate on different clock domains. It ensures that data written in one clock domain is read correctly in another clock domain without data corruption.
